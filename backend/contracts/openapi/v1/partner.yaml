{
  "openapi": "3.1.0",
  "info": {
    "title": "TAKEIN Partner API",
    "version": "1.0.0",
    "description": "Reserved contract namespace. The current Go partner route registrar intentionally registers no endpoints, and no partner authentication flow is active."
  },
  "servers": [
    {
      "url": "https://api.takein.id/api",
      "description": "Future partner API ingress; no active partner paths"
    }
  ],
  "x-contract-status": "reserved-no-active-routes",
  "paths": {},
  "components": {
    "securitySchemes": {
      "partnerToken": {
        "type": "http",
        "scheme": "bearer",
        "description": "Reserved only; not currently issued or accepted by an active partner endpoint."
      }
    }
  }
}
